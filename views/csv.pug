extends layout

block content
  div(class="container")
    form(method="POST" action="/csv" enctype="multipart/form-data")
      input(type="file" name="csv" id="csv")
      input(type="submit" class="btn btn-primary mt15")
